{
  "name": "udp_examples",
  "version": "1.0.0",
  "main": "discover.js",
  "scripts": {
    "build": "tsc",
    "clean-build": "rm build/** && npm run build",
    "format": "prettier --write 'src/**/*.{ts,tsx,js,json,md}'",
    "set-group": "node build/set-group.js",
    "sandbox": "node build/sandbox.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node build/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/express": "^5.0.2",
    "@types/node": "^22.15.29",
    "nodemon": "^3.1.10",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
